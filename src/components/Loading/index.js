import { LoadAnimationBlue } from "assets";
import React from "react";

const Loading = () => {
  return (
    <div className="text-center my-3">
      <img src={LoadAnimationBlue} width={30} alt="load-animation-blue" />
    </div>
  );
};

export default Loading;
