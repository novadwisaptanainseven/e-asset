
export const goToTambah = (history) => {
	alert("Go to tambah");
};

export const goToEdit = (id, history) => {
	alert("Go to edit, id: " + id);
};

export const goToDetail = (id, history) => {
	alert("Go to detail, id: " + id);
};

export const handleDelete = (id, history) => {
	alert("Handle delete, id: " + id);
};
